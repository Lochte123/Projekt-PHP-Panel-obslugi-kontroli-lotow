{% extends 'base.html.twig' %}

{% block body %}
    <h1 style="font-size: 30px">Linia lotnicze</h1>

<div id="nowy2">
    <table>
        <tbody>
            <tr>
                <th style="font-size: large">Id</th>
                <td style="font-size: medium">{{ linie_lotnicze.id }}</td>
            </tr>
            <tr>
                <th style="font-size: large">Nazwa</th>
                <td style="font-size: medium">{{ linie_lotnicze.nazwa }}</td>
            </tr>
            <tr>
                <th style="font-size: large">Liczbasamolotow</th>
                <td style="font-size: medium">{{ linie_lotnicze.liczbaSamolotow }}</td>
            </tr>
            <tr>
                <th style="font-size: large">Liczbapracownikow</th>
                <td style="font-size: medium">{{ linie_lotnicze.liczbaPracownikow }}</td>
            </tr>
        </tbody>
    </table>
    <div/>

    <div id="powrot1">

    <ul>
        <li>
            <a href="{{ path('linie_lotnicze_index') }}" style="font-size: medium">Powrot</a>
        </li>
        <li>
            <a href="{{ path('linie_lotnicze_edit', { 'id': linie_lotnicze.id }) }}" style="font-size: medium">Edytuj</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Usun">
            {{ form_end(delete_form) }}
        </li>
    </ul>
        <div/>
{% endblock %}
