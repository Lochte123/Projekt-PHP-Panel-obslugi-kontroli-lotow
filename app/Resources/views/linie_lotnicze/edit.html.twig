{% extends 'base.html.twig' %}

{% block body %}
    <h1 style="font-size: 30px">Linie lotnicze edycja</h1>

<div id="nowy2">
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edytuj" />
    {{ form_end(edit_form) }}
    <div/>

    <ul>
        <li>
            <a href="{{ path('linie_lotnicze_index') }}" style="font-size:medium">Powrot</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Usun">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
